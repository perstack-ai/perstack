{
  "name": "perstack",
  "version": "0.0.46",
  "description": "PerStack CLI",
  "author": "Wintermute Technologies, Inc.",
  "license": "Apache-2.0",
  "type": "module",
  "publishConfig": {
    "access": "public",
    "bin": {
      "perstack": "dist/bin/cli.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm run clean && tsup --config ./tsup.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@perstack/api-client": "workspace:*",
    "@perstack/claude-code": "workspace:*",
    "@perstack/core": "workspace:*",
    "@perstack/cursor": "workspace:*",
    "@perstack/gemini": "workspace:*",
    "@perstack/runtime": "workspace:*",
    "@perstack/tui": "workspace:*",
    "commander": "^14.0.2",
    "dotenv": "^17.2.3",
    "smol-toml": "^1.5.2",
    "ts-dedent": "^2.2.0"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22.0.5",
    "@types/node": "^24.10.1",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
